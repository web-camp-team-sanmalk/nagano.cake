<h2>カート</h2>
<div>
 <div>
   <div>
     <%= link_to "カートを空にする", cart_items_destroy_all_path, method: :delete %>
   </div>
   <div>
     <table class='table'>
      <thead>
        <tr>
          <th>商品名</th>
          <th>単価（税込）</th>
          <th>数量</th>
          <th>小計</th>
          <th></th>
        </tr>
      </thead>
      <tbody>
        <% @items.each do |item| %>
          <tr>
            <td><%= image_tag item.image, size: '250x250' %><%= item.name %></td>
            <td><%= item.add_tax_price.to_s(:delimited) %></td>
            <td></td>
            
            <td><%= item.add_tax_price.to_s(:delimited) %></td>
            
            <td><%= link_to "削除する", cart_item_path(item.id), method: :delete %></td>
            
          </tr>
        <% end %>
      </tbody>
    </table>
    <div>
       <%= link_to "買い物を続ける", root_path  %>
    </div>
    <table>
    <tbody>
      
    </tbody>
    </table>
    <div>
      <%= link_to "情報入力に進む", new_order_path %>
    </div>
    
   </div>
  </div>
 </div>
 

