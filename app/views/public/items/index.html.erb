
<div class="container">
  <div class="row">
    <h2>商品一覧<span class="small">(全 <%= @items.count %> 件)</span></h2>
  </div>
    <div class="row">
      <% @items.each do |item| %>
        <%= link_to item_path(item.id) do %>
          <%= image_tag item.image, size: '250x250' %>
          <p class=""><%=item.name %></p>
          <p class="">¥<%= item.price.to_s(:delimited) %></p>
      <% end %>
        <% end %>
   </div>
 </div>



